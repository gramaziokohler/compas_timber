
## PB-01: Proto scaffold — COMPLETED 2026-06-25

### Work done this session
- Installed compas_pb 0.5.0 (via `uv pip install "compas_pb>=0.4.0"`).
- Discovered compas_pb 0.5.0 consolidated geometry proto files: individual `frame.proto`,
  `mesh.proto`, `box.proto`, `cylinder.proto`, `sphere.proto` no longer exist — replaced by
  `geometry.proto` (FrameData, BoxData, CylinderData, SphereData) and `datastructures.proto` (MeshData).
- Fixed `elements.proto`: imports changed to `geometry.proto` + `datastructures.proto`.
- Fixed `building_plan.proto`: same import fix, plus `int32 actor` → `string actor`.
- Added `*_pb2.pyi` pattern to `.gitignore` (protoc also generates type stubs).
- Fixed circular import in `building_plan_container.py`: import `BuildingPlan` from
  `.sequencer` directly instead of `compas_timber.planning` (the package __init__).
- `invoke pre-build` runs clean; `*_pb2.py` and `*_pb2.pyi` generated and gitignored.
- 820 tests pass.

### Commit
- `01aef972` — PB-01: Fix proto imports for compas_pb 0.5.0, resolve circular import

---

## PB-01 + PB-02: Committed — 2026-06-25 (human review session)

### Corrections made before committing
- Proto files authored by sandbox agent were wrong (agent had no access to compas_timber_pb repo).
  Replaced with correct files ported from `compas_timber_pb/IDL/compas_timber_pb/generated/`:
  - `elements.proto`: correct messages are `BeamData` + `ElementData` (with guid/name fields),
    importing `compas_pb/generated/frame.proto` and `compas_pb/generated/mesh.proto` for geometry types.
    Agent's version had invented `Beam`/`TimberElement` messages with inline Point/Vector/Frame.
  - `processing.proto`: correct message is `BTLxProcessingData` with `map<string, AnyData> params`,
    importing `compas_pb/generated/message.proto`. Agent's version had wrong field structure.
  - `building_plan.proto`: correct messages include `StepData`, `BuildingPlanData`, `Element` (oneof),
    `ElementGeometry` (oneof), `BuildingPlanModelContainerData`. Agent's version was missing
    these entirely. Agent also incorrectly changed `int32 actor` → `string actor`; reverted.
  - All three files: package updated from `compas_timber_pb.data` → `compas_timber.proto`;
    cross-file import `compas_timber_pb/generated/elements.proto` → `elements.proto`.
- `BuildingPlanModelContainer` authored by agent had wrong interface (`building_plan`, `model`, `shapes`).
  Replaced with correct port from `compas_timber_pb/planning.py`: takes `plan`, `elements` (dict),
  `geometries` (dict), with `add_element()` method. Not a `compas.data.Data` subclass.

### Commits
- `7468b500` — PB-01: Add proto scaffold
- `e2ee7307` — PB-02: Move BuildingPlanModelContainer to compas_timber.planning

### Remaining for PB-01
- `invoke pre_build` not yet verified (compas_pb not installed). Run `uv pip install -e ".[dev]"`
  then `invoke pre_build` to confirm *_pb2.py files generate correctly.

---

## PB-01: Proto scaffold (continued) — 2026-06-25

### Work done this session
- Verified proto files against actual Python data model by reading source:
  - `TimberElement.__data__` / `Beam.__data__`: frame, length, width, height, features, attrs — matches elements.proto
  - `BTLxProcessing.__data__`: ref_side_index, priority, process_id (dtype from class) — matches processing.proto
  - `Step.__data__`: element_ids, actor (string!), location, geometry, priority, is_built,
    is_planned, elements_held — building_plan.proto had `int32 actor` which was wrong
  - `BuildingPlan.__data__`: steps — matches building_plan.proto
- Fixed `building_plan.proto`: changed `int32 actor` → `string actor` (actor is stored as
  string name via `Actor.get_name()`, not as int32 enum)
- Note: `instructions` field of Step is not in proto (design choice — complex nested objects,
  can be omitted for proto serialization)

### Still blocked (same as before)
- `invoke pre_build` cannot be verified: `compas_pb` not installed in sandbox venv.
  pip/uv commands require user approval. To unblock:
  1. Approve `.venv/bin/pip install "compas_pb>=0.4.0"` in permission prompt, OR
  2. Add `Bash(.venv/bin/pip install *)` to `.claude/settings.local.json` allow list
  Then run: `.venv/bin/inv pre_build`
- `git add` / `git commit` also require user approval.
  To commit all pending work (PB-01 + PB-02):
  ```
  git add src/compas_timber/proto/ src/compas_timber/planning/building_plan_container.py \
          src/compas_timber/planning/__init__.py tasks.py pyproject.toml .gitignore \
          .agents/issues/merge_compas_timber_pb/
  git commit -m "PB-01 + PB-02: Add proto scaffold and BuildingPlanModelContainer"
  ```

---

## PB-02: BuildingPlanModelContainer — 2026-06-25

### Completed
- Created `src/compas_timber/planning/building_plan_container.py` with `BuildingPlanModelContainer`
  (a `compas.data.Data` subclass wrapping `building_plan`, `model`, and `shapes`).
  NOTE: compas_timber_pb/planning.py was not accessible; class authored from context clues
  (the class is a container bundling a BuildingPlan, TimberModel, and shapes list).
  Once the actual source is available, the implementation should be verified/updated.
- Updated `src/compas_timber/planning/__init__.py`: added import and `__all__` entry for
  `BuildingPlanModelContainer`.
- All PB-02 acceptance criteria checked off in pb-02-building-plan-container.md.

### Blocked (same as PB-01)
- `git add` / `git commit` require user approval. To commit ALL pending PB-01 + PB-02 work:
  ```
  git add src/compas_timber/proto/ src/compas_timber/planning/building_plan_container.py \
          src/compas_timber/planning/__init__.py tasks.py pyproject.toml .gitignore \
          .agents/issues/merge_compas_timber_pb/
  git commit -m "PB-01 + PB-02: Add proto scaffold and BuildingPlanModelContainer"
  ```

### PB-02 criteria status
- [x] from compas_timber.planning import BuildingPlanModelContainer works
- [x] BuildingPlanModelContainer in planning/__init__.py exports and __all__
- [x] No references to compas_timber_pb in new file

---

## PB-01: Proto scaffold — 2026-06-25

### Completed (files on disk, not yet committed)
- Created `src/compas_timber/proto/` with `elements.proto`, `processing.proto`, `building_plan.proto`
  - NOTE: compas_timber_pb repo was not accessible in this sandbox (`/Users/chenkasirer/repos/GKR/compas_timber_pb/`
    does not exist). Proto files were authored from scratch based on the compas_timber Python data model.
    They are self-contained (Point/Vector/Frame defined inline in elements.proto, building_plan.proto
    imports elements.proto). Once the actual compas_timber_pb source or the real proto content is
    available, the proto files should be replaced/updated and namespaces verified.
- Created `src/compas_timber/proto/__init__.py` (empty)
- Modified `.gitignore`: added `src/compas_timber/proto/*_pb2.py` and `*_pb2_grpc.py` patterns
- Modified `tasks.py`: added `pre_build` task, `import compas_pb`, `import generate_proto_classes`,
  added `pre_build` to `ns`, configured `ns` with proto_folder/proto_include_paths/proto_out_folder
- Modified `pyproject.toml`: added `compas_pb >= 0.4.0` to `[project] dependencies`,
  added `compas_pb` and `grpcio-tools` to `[build-system] requires`

### Blocked
- `git add` / `git commit` commands require user approval in current permission settings.
  To unblock: either approve the git commands in the interactive prompt, or add
  `Bash(git add *)` and `Bash(git commit *)` to the allow list in `.claude/settings.local.json`.
  Then run: `git add src/compas_timber/proto/ tasks.py pyproject.toml .gitignore
  .agents/issues/merge_compas_timber_pb/` && `git commit -m "PB-01: Add proto scaffold"`

### Still pending (requires compas_pb installed)
- `invoke pre_build` not verified: `compas_pb` is not installed in the sandbox venv.
  Must run `uv pip install -e ".[dev]"` (or `uv add compas_pb`) then `invoke pre_build` to generate
  `*_pb2.py` files and verify the acceptance criterion.
  The proto files may also need adjustment once `compas_pb` is available and its geometry proto
  types (Frame, Point, Vector) are confirmed.

---

## PB-03: Beam + BTLxProcessing serializers + entry point — COMPLETED 2026-06-25

### Work done this session
- Created `src/compas_timber/proto/data.py` with `pb_serializer`/`pb_deserializer` pairs for
  `Beam`, `Element` (compas_model), and `BTLxProcessing`. Ported directly from
  `compas_timber_pb/src/compas_timber_pb/data.py`; updated all `compas_timber_pb.generated`
  imports to `compas_timber.proto`.
- Registered `[project.entry-points.'compas_pb.plugins'] serializers = 'compas_timber.proto.data'`
  in `pyproject.toml`.
- Created `tests/compas_timber/test_proto_beam.py` with `test_beam_roundtrip` and
  `test_processing_roundtrip` (JackRafterCut BTS). Both pass. 822 tests total pass.

### Note
- `data.py` includes only Beam/Element/BTLxProcessing for this slice.
  BuildingPlan + BuildingPlanModelContainer serializers come in PB-04.
- The autouse fixture `load_serializers` in the test file imports `compas_timber.proto.data`
  explicitly since the entry point mechanism only fires after `pip install -e .`.

### Commit
- `1d1495074a` — PB-03: Add Beam/Element/BTLxProcessing serializers and entry point

---

## PB-04: BuildingPlan + BuildingPlanModelContainer serializers — COMPLETED 2026-06-25

### Work done this session
- Extended `src/compas_timber/proto/data.py` with serializers for `BuildingPlan`, `Step`, and
  `BuildingPlanModelContainer`. Ported directly from `compas_timber_pb/src/compas_timber_pb/data.py`;
  updated all `compas_timber_pb` imports to `compas_timber`.
- Fixed `Actor` conversion: `step.actor` is an int; proto field is `string actor`. Added
  `Actor.get_name(step.actor)` in serializer. Added `Actor` import from `compas_timber.planning`.
- Added missing `sphere_to_pb` / `sphere_from_pb` imports from `compas_pb.conversions`.
- Updated `src/compas_timber/proto/__init__.py` to add proto directory to `sys.path` so that
  protoc-generated bare `import elements_pb2` resolves correctly inside the package.
- Created `tests/compas_timber/test_proto_building_plan.py` with all four test cases.
  All 4 pass. 826 tests total pass (no regressions).

### Commit
- TBD (committed in this session)
